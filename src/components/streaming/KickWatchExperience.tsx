"use client";

/**
 * Streaming module entry — re-exports the watch experience with LiveKit overlay + Stripe subs.
 * Implementation lives in `kick-home/KickWatchExperience` (discovery hub unchanged).
 */
export { default } from "@/components/kick-home/KickWatchExperience";

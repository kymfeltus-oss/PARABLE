/**
 * Next.js uses `src/app/layout.tsx`, not a root React `App.tsx`.
 * The live platform shell is `src/components/layout/ParableGlobalLayout.tsx`:
 * GlobalPulseProvider → ParableGlobalHeader → profile-aware main workspace.
 */
export {};
